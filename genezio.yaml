<<<<<<< HEAD
# The name of the project.
name: fullstack-ecommerce
# The region where the project is deployed. Available regions: us-east-1, eu-central-1, eu-west-1
region: us-east-1
# The version of the Genezio YAML configuration to parse.
yamlVersion: 2
backend:
  # The root directory of the backend.
  path: ./api
  # Information about the backend's programming language.
  language:
    # The name of the programming language.
    name: js
    # The package manager used by the backend.
    packageManager: npm
  scripts:
    deploy:
      - npm install && npm run build
  # Information about the backend's functions.
  functions:
    # The name (label) of the function.
    - name: ecom-expres-api-function
      # The path to the function's code.
      path: ./
      # The entry point for the function.
      entry: app.mjs
      # The name of the function handler.
      handler: handler
      # The compatibility of the function handler.
      type: httpServer
=======
backend:
    functions:
        - entry: dist/src/index.js
          handler: handler
          name: express
          path: .
          type: httpServer
    language:
        name: js
        runtime: nodejs20.x
    path: api
    scripts:
        deploy:
            - npm install
            - npm run build
        local:
            - npm install
            - npm run build
name: E-commerce
region: us-east-1
yamlVersion: 2
>>>>>>> 05b4adbae9f0e4c31815a852015534ed3dd304e9
